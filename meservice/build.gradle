jar {
    baseName = 'meservice'
}

dependencies {
    compile project(':merepository')
    compile project(':medto')
    compile project(':memapper')

    testCompile ("org.springframework.boot:spring-boot-starter-test:${springBootStarterTestVersion}")
}

apply from: 'deploy.gradle'